{
  "name": "flask-react",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run api\" \"npm run client\"",
    "api": "FLASK_APP=api flask run",
    "client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
